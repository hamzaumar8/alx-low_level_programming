#include <stdio.h>

/**
 * main - prints the lower alphabet in reverse
 * followed by a new line
 * Return: 0
 */
int main(void)
{
	char wds;

	for (wrds = 'z'; wds >= 'a'; wds--)
	{
		putchar(wds);
	}
	putchar('\n');
	return (0);
}
